<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="icon" type="image/x-icon" href="/img/vote.png">
    <style>
      body{
        background-size: cover;
      }
      .u{
        text-decoration: underline;
      }
      .one{
        display: flex;
        flex-direction: row;
      }
      .two{
        display: flex;
        flex-direction: row;
      }
      .three{
        display: flex;
        flex-direction: row;
        width: 100%;
        justify-content: center;
        align-items: center;
      }
      .four{
        display: flex;
        flex-direction: row;
        width: 100%;
        justify-content: center;
        align-items: center;
      }
      .boxx {
        background-color:	#A9A9A9;
        width: 300px;
        border: 10px solid #282a35;
        padding: 40px;
        margin: 20px;
        text-align: center;
      }
      .round{
        background-color: #282a35;
        border-radius: 100px;
        padding: 50px;
      }
      .a{
        display: flex;
        flex-direction: column;
        text-align: left;
      }
    </style>
    <!-- <link rel="stylesheet" href="./styles" /> -->
    <title><%= title %></title>
    <!-- index page,that is, this is the page where the user first visits the website -->
  </head>
  <body class="bg-gray-50" background="img/image2.jpg">
    <nav>
      <div class="container flex flex-wrap items-center justify-between mx-auto">
        <a href="/" class="py-2 px-4 flex items-center">
          <span class="self-center text-xl font-semibold whitespace-nowrap text-MidnightBlue-400 hover:text-blue-400">E-voting</span>
        </a>
      </div>
    </nav>
    <div>
      <% if (messages && messages.error && messages.error.length > 0) { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
              <p class="bg-white my-3 list-none rounded-md px-4 py-2 font-serif"><%= messages.error[i] %></p>
          <% } %>
      <% } %>
  </div>
    <div class="grid grid-cols-7">
      <div class="col-start-3 col-span-3">
        <p class="py-5 mb-10 text-4xl font-serif text-MidnightBlue-400 u">ONLINE VOTING PLATFORM</p>
        <div class="boxx">
          <p class="py-1 text-xl py-3 mb-9 text-white">
            <a class="round py-3 mb-9 text-green-400 font-serif hover:text-blue-400" href="/signup">Sign-up</a>
          </p>
          <p class="py-1 text-xl font-serif py-3 mb-9 text-white">
            <a class="round text-green-400 font-serif hover:text-blue-400 py-3 mb-9" href="/login">Sign-in</a>
          </p>
        </div>
      </div>
    </div>
  </body>
</html>
